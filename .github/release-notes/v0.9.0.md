# SpectraLLM v0.9.0

Une version de fiabilité, centrée sur le moment le plus fragile de l'installation : **récupérer
les modèles**. Un transfert interrompu ne peut plus passer pour un succès, ce qui arrive est
vérifié avant d'être accepté, et la source peut être déplacée. S'y ajoutent un démarrage sans
compilation (`start.sh --hub`) et deux correctifs sur la publication elle-même. Le
[CHANGELOG](../../CHANGELOG.md) fait foi.

---

## ⬇️ Téléchargement des modèles — complet, ou rien

Les modèles par défaut pèsent ~4,8 Go. Sur une connexion domestique, une coupure en cours de
transfert n'est pas un cas limite : c'est le cas courant.

- **Le fichier définitif n'existe que s'il est complet.** `setup.sh` et `setup.bat` écrivaient
  directement sur le nom attendu : une coupure, un Ctrl-C ou un disque plein y laissaient un GGUF
  tronqué. Le lancement suivant, qui ne teste que la PRÉSENCE du fichier, annonçait « [OK] présent »,
  la pile démarrait, et la panne ressortait ailleurs — `llama-server` refusant le modèle, ou
  l'entrypoint attendant un fichier qui existe. Les deux scripts passent désormais par un
  sous-programme unique qui télécharge vers `.part` et ne renomme qu'au succès.
- **La reprise est possible** : `-C -` repart de l'octet où l'on s'était arrêté — sur 4,7 Go, la
  différence entre reprendre 10 % et retélécharger 90 %. Trois tentatives couvrent la connexion
  coupée en plein transfert, que le `--retry` de curl ignore.
- **`setup.bat` avait sa propre variante du défaut** : il lui manquait `--fail`. Sans ce drapeau,
  une 404 ou une page d'erreur HTML était enregistrée comme si c'était le modèle, curl sortait en 0,
  et le script annonçait « [OK] telecharge ». Un GGUF de 12 Ko contenant du HTML, sur Windows
  uniquement — l'angle mort exact de `test_windows_scripts_parity`, qui compare les options
  annoncées et non ce qu'elles font.

## 🔍 Ce qu'on reçoit est vérifié avant d'être accepté

Un miroir mal configuré, un proxy d'entreprise ou un portail captif renvoient une page en **200** :
`--fail` la laisse passer, et le « modèle » obtenu n'est rejeté qu'au démarrage de llama.cpp,
plusieurs étapes plus loin.

- **Les quatre premiers octets d'un GGUF** suffisent à le savoir tout de suite.
- **L'empreinte SHA-256** est vérifiée si elle est épinglée (`SPECTRA_EMBED_MODEL_SHA256`,
  `SPECTRA_CHAT_MODEL_SHA256`), affichée sinon — même convention que `LLMFIT_SHA256` côté image,
  pour qu'épingler ne demande que de recopier la valeur obtenue.
- Les deux contrôles ont lieu **avant le renommage** : ce qui échoue ne porte jamais le nom définitif.

## 🔀 La source des modèles est déplaçable

- **`HF_ENDPOINT`** — déjà transmise au reranker et au trainer par `docker-compose.yml` — construit
  désormais aussi les URL des scripts de setup. Un miroir se déclare une fois et vaut pour toute la
  pile, plutôt qu'une seconde variable pour le même besoin.
- Pour une source qui n'est pas un miroir HuggingFace, **`SPECTRA_EMBED_MODEL_URL`** et
  **`SPECTRA_CHAT_MODEL_URL`** remplacent l'URL entière, comme `SPECTRA_RERANKER_ONNX_URL` le fait
  déjà pour l'artefact du reranker.

## 🚀 `start.sh --hub` — démarrer sans rien construire

Les images sont publiées depuis la v0.8.0, mais le chemin pour les utiliser n'existait que dans la
documentation Docker Hub — or personne n'arrive par Docker Hub, on arrive par GitHub. Le README
proposait toujours `./scripts/start.sh --first-run`, qui **compile**. Le drapeau `--hub` tire les
images publiées, à côté de la commande que les gens lisent déjà.

## 📝 Documentation — une taille fausse d'un facteur quatre

Le README annonçait « ~1,2 Go » là où le seul modèle de chat en pèse 4,7. Les manuels et le guide de
démarrage décrivaient encore un défaut « Phi-4-mini (~1,1 Go) » sous une commande qui télécharge
Qwen2.5-7B — étiquette restée d'un changement de modèle par défaut. C'est la phrase qui décide si
l'utilisateur lance la commande maintenant ou la remet à plus tard.

## 🏷️ Publication — deux garde-fous rendus bloquants

- **Une release sur deux partait avec le corps générique.** `release.yml` avertissait quand
  `.github/release-notes/<tag>.md` manquait, puis publiait la liste brute des PR fusionnées. Un
  avertissement ne bloque rien, et personne ne lit le journal d'une release réussie : la v0.7 puis la
  v0.8.0 sont parties ainsi. Le workflow échoue maintenant, avec l'échappatoire explicite
  `allow_generated_notes`, et un test dit l'écart AVANT le tag.
- **L'overlay Docker Hub épingle la version au lieu de suivre `latest`.** `latest` sert ce qui a été
  publié le jour du démarrage, pas ce que cette copie du dépôt a été testée avec — le raisonnement
  qui avait déjà fait épingler llama.cpp et chromadb. Un contrôle compare le défaut au dernier tag
  git : périmé, il ne casse rien, il sert juste une vieille version en silence.

## Mise à jour

Depuis les images publiées, sans rien construire :

```bash
docker compose --project-directory . \
  -f deploy/docker/docker-compose.yml -f deploy/docker/docker-compose.hub.yml pull
docker compose --project-directory . \
  -f deploy/docker/docker-compose.yml -f deploy/docker/docker-compose.hub.yml \
  up -d --wait --no-build
```

Ou comme avant : `git pull` puis `./scripts/start.sh --build`. Nouveau raccourci depuis cette
version : `./scripts/start.sh --hub` démarre depuis les images publiées sans compiler.

> **Si un téléchargement de modèle avait échoué avant cette version** : un GGUF tronqué peut encore
> occuper l'emplacement attendu, et il porte le nom définitif. Le contrôle d'empreinte n'y change
> rien — il ne s'applique qu'aux transferts qui passent par le nouveau chemin. Supprimez le fichier
> suspect et relancez `setup` : la reprise repartira proprement.

## Versions incluses

| Version interne | Résumé |
|---|---|
| section « Non publié » au-dessus de 1.13.0 | Téléchargement des modèles atomique et repris, vérification GGUF et SHA-256, source déplaçable (`HF_ENDPOINT`, URL explicites), `start.sh --hub`, tailles de modèles corrigées dans la documentation, notes de release et épinglage de l'overlay rendus bloquants |
